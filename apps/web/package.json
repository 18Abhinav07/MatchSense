{
  "name": "@matchsense/web",
  "version": "0.0.0",
  "private": true,
  "description": "MatchSense progressive web application shell.",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
